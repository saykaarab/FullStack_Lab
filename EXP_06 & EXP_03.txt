== EXPERIMENT 06 ==

In MainActivity.java ->

package com.example.experiment6;

import android.app.AlertDialog;
import android.os.Bundle;
import android.view.MotionEvent;
import android.view.View;
import android.widget.ArrayAdapter;
import android.widget.Button;
import android.widget.Spinner;
import android.widget.TextView;
import android.widget.Toast;

import androidx.appcompat.app.AppCompatActivity;

public class MainActivity extends AppCompatActivity {

    Spinner spinnerPlanet;
    Button btnToast, btnAlert, btnPopup;
    TextView txtTouch;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        spinnerPlanet = findViewById(R.id.spinnerPlanet);
        btnToast = findViewById(R.id.btnToast);
        btnAlert = findViewById(R.id.btnAlert);
        btnPopup = findViewById(R.id.btnPopup);
        txtTouch = findViewById(R.id.txtTouch);

        String[] planets = {
                "Mercury",
                "Venus",
                "Earth",
                "Mars",
                "Jupiter"
        };

        ArrayAdapter<String> adapter =
                new ArrayAdapter<>(
                        this,
                        android.R.layout.simple_spinner_item,
                        planets);

        adapter.setDropDownViewResource(
                android.R.layout.simple_spinner_dropdown_item);

        spinnerPlanet.setAdapter(adapter);

        btnToast.setOnClickListener(v -> {

            String selected =
                    spinnerPlanet.getSelectedItem().toString();

            Toast.makeText(
                    MainActivity.this,
                    "Selected: " + selected,
                    Toast.LENGTH_SHORT
            ).show();

        });

        btnAlert.setOnClickListener(v -> {

            AlertDialog.Builder builder =
                    new AlertDialog.Builder(MainActivity.this);

            builder.setTitle("Alert");
            builder.setMessage("Do you want to continue?");

            builder.setPositiveButton("Yes",
                    (dialog, which) -> {
                        Toast.makeText(
                                MainActivity.this,
                                "You clicked YES",
                                Toast.LENGTH_SHORT
                        ).show();
                    });

            builder.setNegativeButton("No",
                    (dialog, which) -> {
                        Toast.makeText(
                                MainActivity.this,
                                "You clicked NO",
                                Toast.LENGTH_SHORT
                        ).show();
                    });

            builder.show();

        });

        btnPopup.setOnClickListener(v -> {

            AlertDialog.Builder popup =
                    new AlertDialog.Builder(MainActivity.this);

            popup.setTitle("Popup");
            popup.setMessage("This is a Simple Popup");

            popup.setPositiveButton("OK",
			
                    (dialog, which) -> {
                    });

            popup.show();

        });

        txtTouch.setOnTouchListener(
                new View.OnTouchListener() {

                    @Override
                    public boolean onTouch(
                            View v,
                            MotionEvent event) {

                        Toast.makeText(
                                MainActivity.this,
                                "Touch Detected",
                                Toast.LENGTH_SHORT
                        ).show();

                        return true;
                    }
                });
    }
}

---------------------------------------------------------------------------------------------
In activity_main.xml->

<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select a Planet"
        android:textSize="20sp"/>

    <Spinner
        android:id="@+id/spinnerPlanet"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <Button
        android:id="@+id/btnToast"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Toast"/>

    <Button
        android:id="@+id/btnAlert"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Alert"/>

    <Button
        android:id="@+id/btnPopup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Popup"/>

    <TextView
        android:id="@+id/txtTouch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Touch Me"
        android:textSize="22sp"
        android:padding="20dp"/>

</LinearLayout>

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
EXP 03 ->

# Step 1: Create SecondActivity

Right Click:

```text
kotlin+java
 └─ com.example.intent
```

→ New → Activity → Empty Views Activity

Name:

```text
SecondActivity
```

Click Finish.

---

# Step 2: activity_main.xml

Replace everything with:

```xml
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to First Activity"
        android:textSize="24sp"/>

    <Button
        android:id="@+id/btnNext"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Go To Second Activity"/>

</LinearLayout>
```

---

# Step 3: activity_second.xml

Replace everything with:

```xml
<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to Second Activity"
        android:textSize="24sp"/>

    <Button
        android:id="@+id/btnBack"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Back"/>

</LinearLayout>
```

---

# Step 4: MainActivity.java

Replace everything inside MainActivity with:

```java
package com.example.intent;

import android.content.Intent;
import android.os.Bundle;
import android.widget.Button;

import androidx.appcompat.app.AppCompatActivity;

public class MainActivity extends AppCompatActivity {

    Button btnNext;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);

        btnNext = findViewById(R.id.btnNext);

        btnNext.setOnClickListener(v -> {

            Intent i = new Intent(
                    MainActivity.this,
                    SecondActivity.class);

            startActivity(i);

        });
    }
}
```

---

# Step 5: SecondActivity.java

Replace everything with:

```java
package com.example.intent;

import android.os.Bundle;
import android.widget.Button;

import androidx.appcompat.app.AppCompatActivity;

public class SecondActivity extends AppCompatActivity {

    Button btnBack;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_second);

        btnBack = findViewById(R.id.btnBack);

        btnBack.setOnClickListener(v -> {

            finish();

        });
    }
}
```

---

# Expected Output

### Screen 1

```text
Welcome to First Activity

[Go To Second Activity]
```

Click button

↓

### Screen 2

```text
Welcome to Second Activity

[Back]
```

Click Back

↓

Returns to Screen 1

---

### If Android Studio creates code like:

```java
EdgeToEdge.enable(this);
ViewCompat.setOnApplyWindowInsetsListener(...)
```

you can leave it there or remove it. It won't affect the experiment.

If you get any error after creating `SecondActivity`, send a screenshot and I'll fix it immediately.
