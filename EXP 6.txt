node version
npm version
npm install -g @angular/cli
ng version

------------------------------------------

npx ng new routing-demo
cd routing-demo
npx ng generate component home
npx ng generate component about
npx ng generate component contact
npx ng serve

--------------------------------------------

# 1. app.routes.ts

```typescript
import { Routes } from '@angular/router';

import { Home } from './home/home';
import { About } from './about/about';
import { Contact } from './contact/contact';

export const routes: Routes = [
  { path: 'home', component: Home },
  { path: 'about', component: About },
  { path: 'contact', component: Contact },

  { path: '', redirectTo: 'home', pathMatch: 'full' }
];
```

---

# 2. app.ts

```typescript
import { Component } from '@angular/core';
import { RouterOutlet, RouterLink } from '@angular/router';

@Component({
  selector: 'app-root',
  imports: [RouterOutlet, RouterLink],
  templateUrl: './app.html',
  styleUrl: './app.css'
})
export class App {
}
```

---

# 3. app.html

```html
<h1>Angular Routing Demo</h1>

<nav>
  <a routerLink="/home">Home</a>
  <a routerLink="/about">About</a>
  <a routerLink="/contact">Contact</a>
</nav>

<hr>

<router-outlet></router-outlet>
```

---

# 4. app.css

```css
body {
  margin: 0;
  font-family: Arial, sans-serif;
}

h1 {
  text-align: center;
  color: #333;
  margin-top: 20px;
}

nav {
  text-align: center;
  margin: 20px 0;
}

nav a {
  text-decoration: none;
  color: white;
  background-color: #007bff;
  padding: 10px 20px;
  margin: 0 10px;
  border-radius: 5px;
  transition: 0.3s;
}

nav a:hover {
  background-color: #0056b3;
}

hr {
  margin: 20px 50px;
}
```

---

# 5. home.html

```html
<div class="page-container">
  <h2>Home Page</h2>
  <p>Welcome to Home Page</p>
</div>
```

---

# 6. home.css

```css
.page-container {
  min-height: 70vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  font-family: 'Segoe UI', sans-serif;
  text-align: center;
}

.page-container h2 {
  font-size: 50px;
  color: #dd0031;
  margin-bottom: 15px;
  font-weight: bold;
}

.page-container p {
  font-size: 24px;
  color: #555;
}
```

---

# 7. about.html

```html
<div class="page-container">
  <h2>About Page</h2>
  <p>Learn how Angular routing helps navigate between components seamlessly.</p>
</div>
```

---

# 8. about.css

```css
.page-container {
  min-height: 70vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  font-family: 'Segoe UI', sans-serif;
  text-align: center;
}

.page-container h2 {
  font-size: 50px;
  color: #dd0031;
  margin-bottom: 15px;
  font-weight: bold;
}

.page-container p {
  font-size: 24px;
  color: #555;
}
```

---

# 9. contact.html

```html
<div class="page-container">
  <h2>Contact Page</h2>
  <p>Get in touch with us for any Angular development queries.</p>
</div>
```

---

# 10. contact.css

```css
.page-container {
  min-height: 70vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  font-family: 'Segoe UI', sans-serif;
  text-align: center;
}

.page-container h2 {
  font-size: 50px;
  color: #dd0031;
  margin-bottom: 15px;
  font-weight: bold;
}

.page-container p {
  font-size: 24px;
  color: #555;
}